import streamlit as st
import pandas as pd
from utils import get_gmap_link

def show():
    st.header("🚌 Day 2: 金澤市區巴士一日遊")

    # ==========================================
    # 1. 購買一日券
    # ==========================================
    st.subheader("1️⃣ 金澤車站：購買巴士一日券")
    
    # [版面] 左邊資訊，右邊導航
    col1, col2 = st.columns([3, 1])
    with col1:
        st.markdown("""
        **任務：** 前往 **金澤站東口 (鼓門方向)** 購買「金澤市內巴士一日周遊券」。
        * **地點**：北鐵站前中心 (交通案內所)，靠近東口巴士圓環。
        """)
    with col2:
        st.write("") # 排版用
        st.link_button("🗺️ 導航：購票處", get_gmap_link("Hokutetsu Ticket Office Kanazawa Station", "walking"))

    # [資訊] 票價表格 (來自 PDF 資訊)
    with st.container(border=True):
        st.markdown("#### 🎫 金澤巴士一日周遊券 (One Day Pass)")
        st.caption("憑此券可無限次搭乘：城下町周遊巴士 (RL/LL)、兼六園區間巴士、北鐵路線巴士。")
        
        # 建立票價 Dataframe
        price_data = {
            "票種": ["大人 (Adults)", "小孩 (Children, 6-12歲)"],
            "價格": ["¥800", "¥400"]
        }
        st.table(pd.DataFrame(price_data))
        
        st.info("💡 **小撇步**：搭乘巴士時，後門上車、前門下車。下車時出示票券日期面給司機看即可 [cite: 1, 2, 8]。", icon="🚌")

    st.divider()

    # ==========================================
    # 2. 近江町市場
    # ==========================================
    col_title, col_btn = st.columns([3, 1])
    with col_title:
        st.subheader("2️⃣ 近江町市場 (早餐/早午餐)")
    with col_btn:
        st.write("")
        st.link_button("🗺️ 導航：近江町市場", get_gmap_link("Omicho Market", "transit"))

    st.markdown("""
    * **🚌 交通方式**：
        * **上車**：金澤站東口 **7號月台** 。
        * **路線**：搭乘 **右回 (Right Loop, 紅色)** 或 左回 (Left Loop, 綠色) 皆可。
        * **下車**：`RL1` / `LL1` 近江町市場 (武藏之辻)。
    * **😋 推薦美食**：海鮮丼、生蠔、關東煮、近江町可樂餅。
    """)

    st.divider()

    # ==========================================
    # 3. 東茶屋街
    # ==========================================
    col_title, col_btn = st.columns([3, 1])
    with col_title:
        st.subheader("3️⃣ 東茶屋街 (古都散策)")
    with col_btn:
        st.write("")
        st.link_button("🗺️ 導航：東茶屋街", get_gmap_link("Higashi Chaya District", "transit"))

    st.markdown("""
    * **🚌 交通方式**：
        * **上車**：在近江町市場對面 (MZA百貨前) 搭車。
        * **路線**：推薦搭乘 **右回 (Right Loop)** 往橋場町方向 [cite: 143]。
        * **下車**：`RL4` 橋場町 (Hashiba-cho)。下車後往河邊走即可看到入口。
    """)

    with st.expander("🍦 點我看：必吃美食清單"):
        st.markdown("""
        1.  **箔一 (Hakuichi)**：整片金箔冰淇淋 (視覺效果滿分)。
        2.  **烏雞庵**：金箔蜂蜜蛋糕。
        3.  **不室屋**：寶之麩 (可愛的麩湯包)。
        """)

    st.divider()

    # ==========================================
    # 4. 兼六園
    # ==========================================
    col_title, col_btn = st.columns([3, 1])
    with col_title:
        st.subheader("4️⃣ 兼六園 (日本三大名園)")
    with col_btn:
        st.write("")
        st.link_button("🗺️ 導航：兼六園", get_gmap_link("Kenrokuen Garden", "transit"))

    st.markdown("""
    * **🚌 交通方式**：
        * **上車**：`RL4` 橋場町 (原下車處附近)。
        * **路線**：搭乘 **右回 (Right Loop)**。
        * **下車**：`RL7` 兼六園下‧金澤城 (Kenrokuen-shita)。
    * **📸 重點**：徽軫燈籠 (琴柱燈籠)、雪吊 (冬季限定美景)。
    """)

    st.divider()

    # ==========================================
    # 5. 21世紀美術館
    # ==========================================
    col_title, col_btn = st.columns([3, 1])
    with col_title:
        st.subheader("5️⃣ 21世紀美術館")
    with col_btn:
        st.write("")
        st.link_button("🗺️ 導航：21世紀美術館", get_gmap_link("21st Century Museum of Contemporary Art, Kanazawa", "walking"))

    st.info("🚶 **交通建議：走路** (約 3-5 分鐘)。")
    st.markdown("""
    兼六園 (真弓坂口) 出來過馬路即抵達美術館。
    * **必看**：泳池 (The Swimming Pool) - 上方俯瞰免費，下方需購票/預約。
    * **戶外**：彩色玻璃牆、球型裝置藝術。
    """)

    st.divider()

    # ==========================================
    # 6. 金澤 FORUS (晚餐 & 逛街)
    # ==========================================
    col_title, col_btn = st.columns([3, 1])
    with col_title:
        st.subheader("6️⃣ 金澤 FORUS (晚餐/逛街)")
    with col_btn:
        st.write("")
        st.link_button("🗺️ 導航：金澤 FORUS", get_gmap_link("Kanazawa FORUS", "transit"))

    st.markdown("""
    * **🚌 交通方式**：
        * **上車**：美術館前 `RL8` 廣坂‧21世紀美術館。
        * **路線**：搭乘 **右回 (Right Loop)** 或 **左回 (Left Loop)** 往金澤車站皆可。
        * **下車**：金澤車站東口。
    * **📍 位置**：FORUS 百貨就在金澤車站東口 (鼓門) 左手邊，走路 1 分鐘。
    """)
    
    st.success("🛍️ **購物重點 (同棟大樓)**：\n* **4F**：Uniqlo (免稅)\n* **5F**：寶可夢中心 (Pokemon Center Kanazawa) - 門口有「美納斯」雕像。")

    # 額外按鈕：直接導航去寶可夢中心
    st.link_button("⚡ 導航：寶可夢中心 (5F)", get_gmap_link("Pokemon Center Kanazawa", "walking"))

if __name__ == "__main__":
    show()
